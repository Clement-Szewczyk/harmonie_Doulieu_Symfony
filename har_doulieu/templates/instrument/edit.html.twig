{% extends 'base.html.twig' %}

{% block title %}Edition Instrument{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/instrument.css') }}">
{% endblock %}

{% block body %}
    {% include "navbar.html.twig" %}
    <h1>Editition de {{instrument.NumeroSerie}}</h1>


    {{ include('instrument/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {% for message in app.flashes('error') %}
        <div class="flash-error">
            {{ message }}
        </div>
    {% endfor %}


    {{ include('instrument/_delete_form.html.twig') }} 

    <a href="{{ path('app_instrument_index') }}" class="back">Retour à la liste</a>
{% endblock %}
