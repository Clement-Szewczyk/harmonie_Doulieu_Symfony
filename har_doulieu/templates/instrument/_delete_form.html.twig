<form method="post" action="{{ path('app_instrument_delete', {'id': instrument.id}) }}" onsubmit="return confirm('Voullez-vous supprimer l\'instrument?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instrument.id) }}">
    <button class="submit">Delete</button>
</form>
