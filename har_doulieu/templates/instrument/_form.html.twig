{{ form_start(form) }}
    
    <div class="info">
        {{ form_row(form.numero_serie) }}
        {{ form_row(form.marque)}}
        {{ form_row(form.pupitre)}}
    </div>

    <div class="info">
        {{ form_row(form.locataire_musicien) }}
        {{ form_row(form.locataire_eleves) }}
    </div>

    
    <button class="submit">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
