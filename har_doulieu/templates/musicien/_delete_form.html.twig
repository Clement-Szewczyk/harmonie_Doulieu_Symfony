<form method="post" action="{{ path('app_musicien_delete', {'id': user.id}) }}" onsubmit="return confirm('Voullez-vous supprimer ce musicien ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="submit   ">Supprimer</button>
</form>
